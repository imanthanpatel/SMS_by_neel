export default function StudentDashboard() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-100 p-6">
      <div className="bg-white rounded-xl shadow-sm p-8 max-w-lg text-center">
        <h1 className="text-2xl font-bold text-gray-800 mb-3">Student Dashboard</h1>
        <p className="text-gray-500">
          The uploaded <code>student_dashboard.html</code> contains only the character
          <code>z</code>, so there is no actual dashboard UI to convert yet.
        </p>
      </div>
    </div>
  );
}
